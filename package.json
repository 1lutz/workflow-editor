{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.ts --minify --format=esm --bundle --outdir=src/workflow_editor/static",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"express": "^4.18.2",
		"litegraph.js": "^0.7.18"
	},
	"devDependencies": {
		"@anywidget/types": "^0.1.4",
		"typescript": "^5.3.3",
		"esbuild": "^0.19.11"
	}
}